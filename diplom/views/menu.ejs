<!DOCTYPE html5>
<html>
    <head>
        <link rel="stylesheet" href="/style.css">
    </head>
    <body>
    <div class="registration-cssave">
        <form class="form-main">
            <div class="mmenu">
            <button class="btn btn_mm margin-f" type="button" onclick="location='/vihod'">Выйти</button>
            <button class="btn btn_mm" type="button" onclick="location='/menu/PersonalArea'">Личный кабинет</button>
        </div> <% if (json.length>0) { %>
        <div class="form-group tb_prev tb_prev__padding">
            <div> Выберите дисциплину:</div>
                <% for (let i = 0; i < json.length; i++) { %>
                <div style="padding-top: 5px">
                <button class="btn" type="button" onclick="location='/menu/ChoiceTest?disc_id=<%= json[i].disc_id %>'"><%= json[i].name_disc %></button>
                <% } %>
                </div>
            </div>  
            <% } %>
        </form>
    </div>
    </body>
</html>
